export default function cookMessage(username) {
  return `Not welcome, ${username}!`;
}

export function makeMessage(username) {
  return `Welcome, ${username}!`;
}

export const userNames = ["Mikola", "Alice", "Robin"];
